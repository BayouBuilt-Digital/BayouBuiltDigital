# Local environment files — never commit secrets
.env
.env.local
.env*.local

# Cloudflare Workers local secrets (Supabase keys, etc.)
.dev.vars
.dev.vars.local

# Dependencies & build output
node_modules/
dist/
.wrangler/
